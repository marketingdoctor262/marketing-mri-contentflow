/**
 * Google Cloud Console → API 및 서비스 → 사용자 인증 정보 → OAuth 2.0 클라이언트 ID
 * 승인된 JavaScript 원본: http://localhost:3000 (또는 배포 도메인)
 * 사용 API: Google Analytics Data API, (선택) Google Ads API는 서버 필요
 *
 * 빈 문자열이면 연동 버튼 클릭 시 안내 메시지 + 데모 데이터만 사용 가능합니다.
 */
export const GOOGLE_CLIENT_ID = "";
